{
  "name": "pilgrimpath",
  "version": "1.0.0",
  "description": "End-to-end web application for large crowd events management",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["crowd-management", "event-management", "pilgrimage", "real-time"],
  "author": "PilgrimPath Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
